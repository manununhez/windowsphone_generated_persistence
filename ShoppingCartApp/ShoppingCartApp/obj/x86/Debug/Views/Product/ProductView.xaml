<Page
    x:Class="ShoppingCartApp.Views.ProductView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ShoppingCartApp.Views">

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Panel -->
        <StackPanel Grid.Row="0" Margin="19,0,0,0">
            <TextBlock Text="Persistence App" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,12,0,0"/>
            <TextBlock Text="Product" Margin="0,-6.5,0,26.5" Style="{ThemeResource HeaderTextBlockStyle}" CharacterSpacing="{ThemeResource PivotHeaderItemCharacterSpacing}"/>
        </StackPanel>

        <!--TODO: Content should be placed within the following grid-->
        <Grid Grid.Row="1" x:Name="ContentRoot" Margin="19,9.5,19,0">
            <TextBlock  x:Name="txbQuantity" TextWrapping="Wrap"  Text="0" Margin="0,10,330,455" FontSize="16"  HorizontalAlignment="Right" Width="16"/>
            <TextBlock Text="elementos" TextWrapping="Wrap" Margin="31,9,251,453" FontSize="16" />
            <Button x:ConnectionId='1' x:Name="btnAddProduct" Content="Add Product" HorizontalAlignment="Right" Margin="222,-9,0,0" VerticalAlignment="Top"                            />

            <!--TODO: Content should be placed within the following grid-->
            <Grid Margin="-2,1,12,-3">
                <ListBox x:ConnectionId='2' BorderBrush="#FF141EE4" Height="auto" BorderThickness="2" MaxHeight="580" x:Name="listBoxobj"                                                Margin="10,51,-10,35"                                >
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Background="Gray" Width="480">
                                <Border BorderBrush="White" BorderThickness="1">
                                    <StackPanel Grid.Column="2" Margin="19,0,0,0">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" TextWrapping="Wrap" Text="IdProducto:" FontSize="17"/>
                                            <TextBlock Grid.Column="1" x:Name="IdProductoTxt" Margin="10,0,0,0" TextWrapping="Wrap" Text="{Binding idProduct}" FontSize="17" Foreground="White"/>
                                        </Grid>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" TextWrapping="Wrap" Text="Nombre:" FontSize="17"/>
                                            <TextBlock Grid.Column="1" x:Name="NombreTxt"  Margin="10,0,0,0" TextWrapping="Wrap" Text="{Binding name}" FontSize="17" Foreground="White"/>
                                        </Grid>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" TextWrapping="Wrap" Text="Precio:" FontSize="17"/>
                                            <TextBlock Grid.Column="1" x:Name="PrecioTxt"  Margin="10,0,0,0" TextWrapping="Wrap" Text="{Binding price}" FontSize="17" Foreground="White"/>
                                        </Grid>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" TextWrapping="Wrap" Text="Codigo:" FontSize="17"/>
                                            <TextBlock Grid.Column="1" x:Name="CodigoTxt"  Margin="10,0,0,0" TextWrapping="Wrap" Text="{Binding code}" FontSize="17" Foreground="White"/>
                                        </Grid>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" TextWrapping="Wrap" Text="IdProveedor:" FontSize="17"/>
                                            <TextBlock Grid.Column="1" x:Name="IdProveedorTxt"  Margin="10,0,0,0" TextWrapping="Wrap" Text="{Binding idProvider}" FontSize="17" Foreground="White"/>
                                        </Grid>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" TextWrapping="Wrap" Text="Descripcion:" FontSize="17"/>
                                            <TextBlock Grid.Column="1" x:Name="DescriptionTxt"  Margin="10,0,0,0" TextWrapping="Wrap" Text="{Binding description}" FontSize="17" Foreground="White"/>
                                        </Grid>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Grid>
    </Grid>
</Page>

